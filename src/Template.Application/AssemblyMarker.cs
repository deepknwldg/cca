namespace Template.Application;

/// <summary>
/// Маркерный тип, используемый для поиска сборки (например, при регистрации сервисов
/// через рефлексию). Не содержит логики.
/// </summary>
public sealed class AssemblyMarker { }
